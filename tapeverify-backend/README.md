# TapeVerify AI Backend

Set `DB_URL`, `DB_USER`, `DB_PASS`, and optionally `OPENAI_API_KEY`, then run `mvn spring-boot:run`. Upload CSV files with the headers: `borrower_name,principal_amount,interest_rate,origination_date,maturity_date` (dates are ISO `YYYY-MM-DD`).
