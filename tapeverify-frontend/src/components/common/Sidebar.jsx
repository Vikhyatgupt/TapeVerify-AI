import { NavLink } from 'react-router-dom';
import { useAuth } from '../../context/AuthContext';
const link = ({isActive}) => 'block rounded-lg px-3 py-2 text-sm '+(isActive?'bg-cyan-400/15 font-semibold text-cyan-200':'text-slate-300 hover:bg-slate-800');
export default function Sidebar(){const {user,logout}=useAuth();const role=user?.role;const items=[['/','Dashboard'],['/ingestion','Upload tape',['OPERATOR','REVIEWER','ADMIN']],['/batches','Import history',['OPERATOR','REVIEWER','ADMIN']],['/exceptions','Exception queue',['REVIEWER','ADMIN']],['/verified-loans','Verified records',['DATA_CONSUMER','REVIEWER','ADMIN']],['/audit','Audit trail'],['/users','Users',['ADMIN']]];return <aside className="fixed inset-y-0 w-60 border-r border-slate-800 bg-slate-950 p-5"><div className="mb-8"><p className="text-lg font-bold text-cyan-200">TapeVerify AI</p><p className="mt-1 text-xs text-slate-500">{role?.replace('_',' ')} workspace</p></div><nav className="space-y-1">{items.filter(([, ,roles])=>!roles||roles.includes(role)).map(([to,label])=><NavLink key={to} to={to} end={to==='/'} className={link}>{label}</NavLink>)}</nav><button onClick={logout} className="absolute bottom-6 rounded-lg border border-slate-700 px-3 py-2 text-sm text-slate-300 hover:bg-slate-800">Sign out</button></aside>}
