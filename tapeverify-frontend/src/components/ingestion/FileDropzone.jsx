import { Upload } from 'lucide-react';
export default function FileDropzone({onFile,uploading}){return <label className="panel flex cursor-pointer flex-col items-center border-dashed py-14 text-center hover:border-cyan-500"><Upload className="mb-4 text-cyan-400" size={32}/><p className="font-semibold">{uploading?'Processing tape…':'Choose a loan tape CSV'}</p><p className="mt-2 text-sm text-slate-400">Headers: borrower_name, principal_amount, interest_rate, origination_date, maturity_date</p><input className="hidden" type="file" accept=".csv,text/csv" disabled={uploading} onChange={e=>e.target.files?.[0]&&onFile(e.target.files[0])}/></label>}
