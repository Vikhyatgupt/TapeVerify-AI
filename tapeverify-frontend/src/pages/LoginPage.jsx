import { useState } from 'react'; import { Navigate, useNavigate } from 'react-router-dom'; import { useAuth } from '../context/AuthContext';
export default function LoginPage(){const {user,login}=useAuth();const nav=useNavigate();const [form,setForm]=useState({username:'',password:''}),[error,setError]=useState(''),[busy,setBusy]=useState(false);if(user)return <Navigate to="/" replace/>;async function submit(e){e.preventDefault();setBusy(true);setError('');try{await login(form.username,form.password);nav('/');}catch(e){setError(e.response?.data?.message||'Sign-in failed.');}finally{setBusy(false)}}return <main className="grid min-h-screen place-items-center bg-slate-950 p-5"><form onSubmit={submit} className="panel w-full max-w-md"><p className="label">Secure reviewer workspace</p><h1 className="mt-1 text-3xl font-bold">Sign in to TapeVerify AI</h1><p className="mt-3 text-sm text-slate-400">Use the development administrator supplied through environment variables.</p><label className="mt-6 block text-sm">Username<input required autoComplete="username" className="input mt-1" value={form.username} onChange={e=>setForm({...form,username:e.target.value})}/></label><label className="mt-4 block text-sm">Password<input required type="password" autoComplete="current-password" className="input mt-1" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label>{error&&<p className="mt-4 text-sm text-rose-300">{error}</p>}<button disabled={busy} className="mt-6 w-full rounded-lg bg-cyan-400 px-4 py-2 font-bold text-slate-950 disabled:opacity-50">{busy?'Signing in…':'Sign in'}</button></form></main>}
